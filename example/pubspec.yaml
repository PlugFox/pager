name: pager_example
description: Pager example
publish_to: 'none'
version: 0.0.1-dev

environment:
  sdk: ">=2.10.0 <3.0.0"
  flutter: ">=1.23.0-13.0.pre <1.24.0"

dependencies:
  flutter:
    sdk: flutter
  pager:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
